filebeat.inputs:
- type: filestream
  enabled: true
  paths:
    - /usr/share/filebeat/logs/application.log
    - /usr/share/filebeat/logs/security.log
  fields:
    service: livelihoodCoupon-app # 프로젝트 이름에 맞게 수정

output.logstash:
  hosts: ["logstash:5044"]
